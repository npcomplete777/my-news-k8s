{{- if and .Values.dash0.enabled .Values.dash0.authToken -}}
apiVersion: v1
kind: Secret
metadata:
  name: dash0-auth
  namespace: {{ .Release.Namespace }}
type: Opaque
stringData:
  token: {{ .Values.dash0.authToken | quote }}
{{- end }}
